/**
 * Task: Use the split method to convert the csvData string into an array of fruits by splitting on the comma (`,`).
 */

let csvData = "apple,banana,cherry,date";

console.log(csvData.split(`,`));
