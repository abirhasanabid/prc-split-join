/**
 * Task: Use the join method to create a string where the numbers are separated by a hyphen (-), like "1-2-3-4-5".
 */

let numbers = [1, 2, 3, 4, 5];

const str = numbers.join('-');

console.log(str);